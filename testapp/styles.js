import { StyleSheet } from "react-native"; 

const theme = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#f60',
      alignItems: 'center',
      justifyContent: 'center',
    },
  });
  
export { theme }