import { StyleSheet } from "react-native"; 

export const theme = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#f60',
      alignItems: 'center',
      justifyContent: 'center',
    },
  });
  